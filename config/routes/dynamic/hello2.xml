<routes xmlns="http://camel.apache.org/schema/spring">
    <!-- here we define the bar route -->
    <route id="foo">
        <from uri="timer://timer1?period=500000"/>
        <log message="Hello Other World!"/>
    </route>
</routes>